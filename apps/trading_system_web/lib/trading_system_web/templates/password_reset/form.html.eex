<%= form_for @changeset, @action, fn f -> %>

  <div class="field">
    <%= label f, :email, "邮箱", class: "label" %>
    <div class="control">
      <%= text_input f, :email, class: "input" %>
      <%= error_tag f, :email %>
    </div>
  </div>

  <div class="field">
    <div class="control">
      <button class="button is-info is-fullwidth" type="submit">发送重置密码邮件</button>
    </div>
  </div>
  
<% end %>